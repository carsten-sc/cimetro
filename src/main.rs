use bevy::prelude::*;
use cimetro::*;

fn main() {
    App::new()
        .add_plugins(DefaultPlugins)
        // Plugin-Registrierung (Platzhalter)
        // .add_plugin(core::CorePlugin)
        // .add_plugin(world::WorldPlugin)
        // ...
        .run();
}
